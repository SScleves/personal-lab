# Copy to terraform.tfvars (gitignored) and adjust. Never commit terraform.tfvars.
location             = "westeurope"
storage_account_name = "stlabstatesscleves" # must be globally unique — tweak if taken
